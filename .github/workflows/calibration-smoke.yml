name: Calibration Smoke Test

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/calibration-smoke.yml'
      - '.github/workflows/calibration.yml'

jobs:
  smoke:
    runs-on: ubuntu-22.04
    steps:
      - name: Echo
        run: 'echo "calibration-smoke: runner available"'
